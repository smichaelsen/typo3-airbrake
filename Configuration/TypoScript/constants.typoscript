plugin.tx_airbrake {
  # cat=plugin.tx_airbrake/file; type=string; label=Airbrake Project ID
  projectId =
  # cat=plugin.tx_airbrake/file; type=string; label=Airbrake Project Key
  projectKey =
  # cat=plugin.tx_airbrake/file; type=string; label=Airbrake Host
  host = api.airbrake.io
  # cat=plugin.tx_airbrake/file; type=boolean; label=Keep logfile: Still also log exceptions to typo3temp/logs/ ?
  stillLogExceptionToLogfile = 0
}
